const state = {
    partBrands: []
};

const getters = {
};

const actions = {
};

const mutations = {
    SET_PART_BRANDS (state, { objects }) {
        state.partBrands = objects
    }
};

export default {
    state,
    getters,
    actions,
    mutations
}
